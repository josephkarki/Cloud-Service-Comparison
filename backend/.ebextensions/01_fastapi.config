option_settings:
  aws:elasticbeanstalk:application:environment:
    PORT: 8080
  aws:elasticbeanstalk:environment:proxy:
    ProxyServer: nginx
  aws:elasticbeanstalk:container:python:
    WSGIPath: main:app
```

---

## Step 3 — Make sure requirements.txt has uvicorn

Open `backend/requirements.txt` and make sure these lines exist:
```
fastapi
uvicorn
